extends ../layout
block content
	main(ng-app="climaLaboral")
		section(ng-controller="resultController")
			header
				h1 {{ evaluacion.result.title }}
				p total de preguntas : {{evaluacion.result.respuestas.length}}
			.ui.list
				.item(ng-repeat="item in evaluacion.result.body")
					i.checkmark.icon.vtop
					.content
						.header {{item.titulo}}
						.description
							span.ui.green.ribbon.label respuestas:
						.list
							.item(ng-repeat="respuesta in item.respuestas.split(',')")
								i.check.circle.icon
								.content
									.header 
										span {{respuesta}}
										span  total:  {{countResult({question:respuesta,title:item.titulo})}} | {{ countResult({question:respuesta,title:item.titulo }) * 100 / evaluacion.result.respuestas.length }} %
